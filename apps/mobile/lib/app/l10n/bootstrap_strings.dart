/// English-only bootstrap copy. Replace with generated ARB localizations when
/// product copy and supported locales are approved.
abstract final class BootstrapStrings {
  static const appName = 'FleetLink';
  static const running = 'FleetLink is running.';
}

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "fleetlink-api"
version = "0.1.0"
description = "FleetLink technical API bootstrap"
requires-python = ">=3.12,<3.13"
dependencies = [
  "fastapi>=0.141,<0.142",
  "pydantic-settings>=2.7,<3",
  "pydantic>=2.13,<3",
  "starlette>=1.7,<2",
  "uvicorn>=0.34,<1",
]

[dependency-groups]
dev = [
  "httpx2>=2.13,<3",
  "pytest>=8.3,<10",
  "ruff>=0.11,<1",
  "mypy>=1.15,<2",
]

[tool.hatch.build.targets.wheel]
packages = ["src/fleetlink"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --strict-config --strict-markers"
filterwarnings = ["error"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.12"
strict = true
